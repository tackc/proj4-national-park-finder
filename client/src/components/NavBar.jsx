import React from 'react';
import NavBar from './NavBar.css';
import {Link} from 'react-router-dom';

const NavBar = (props) => {
    return (
        <nav>
            <div>
                Parks | Favorites | Back
            </div>
        </nav>
    )
}

export default NavBar;